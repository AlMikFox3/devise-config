A basic Leave Management System

-> Set up the database
-> Go through the lib/tasks to browse the list of rake tasks to populate the db
-> Look at schema.rb and set up an admin through the console
-> Good to go !